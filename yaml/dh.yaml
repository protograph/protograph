title: DH
actor:
-  A: Alice
-  B: Bob
sequence:
- A:
    B:
      arrow: "$g$, $p$, $A$"
      from: "$A = g^a$ mod $p$"
      to: "$B = g^b$ mod $p$"

- B: { A: { arrow: "$B$", from: "$K = A^b$ mod $p$", to: "$K = B^a$ mod $p$"}}
